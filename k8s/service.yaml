apiVersion: v1
kind: Service

metadata:
  name: kubernetes-dashboard-service

spec:
  type: NodePort

  selector:
    app: kubernetes-dashboard

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080